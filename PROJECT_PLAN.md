# كافيه مزاج — خطة المشروع وتوثيقه

آخر تحديث: 2026-07-05

## الفكرة العامة

موقع ويب فاخر وتفاعلي (وليس تطبيق موبايل) لكافيه مزاج، يخدم عدة أهداف:

1. **عرض هوية الكافيه**: القصة، الأجواء، الطابق الثاني (صالة الألعاب).
2. **منيو تفاعلي بصفحة واحدة** (`/menu`): يُستخدم كوجهة لرابط QR الموضوع على طاولات الكافيه، ليتصفح الزبون المنتجات من جواله بدل المنيو الورقي.
3. **طلب توصيل** (قهوة، حلويات، أراجيل): بدون أي نظام حسابات مستخدمين — سلة بسيطة، ثم نموذج بيانات، ثم إرسال الطلب كرسالة جاهزة عبر واتساب ليأكده فريق الكافيه يدوياً.
4. **عرض خدمات الكافيه وروابط التواصل الاجتماعي**.

الموقع **Mobile-First** (95% آيفون)، لكنه مصمم كموقع ويب حقيقي (Navbar + سكرول بين أقسام) وليس بواجهة تطبيق (لا يوجد Bottom Tab Bar).

## القرارات المعمارية المتفق عليها

| القرار | الخيار المُعتمد | السبب |
|---|---|---|
| هيكلة الموقع | صفحة رئيسية طويلة بالسكرول (أقسام) + صفحات مستقلة لـ `/menu` و `/order` | يحس الزائر إنه موقع تفاعلي حقيقي، ويسهّل ربط QR بصفحة منيو منفصلة |
| اللغتان | next-intl مع رابط لغة صريح (`/ar`, `/en`) | أفضل لمحركات البحث ومشاركة الروابط، والعربية هي اللغة الافتراضية |
| نظام الطلبات | بدون تسجيل دخول/حسابات — سلة محلية (localStorage) + نموذج بيانات + تسليم الطلب كرسالة واتساب جاهزة | الكافيه ما يحتاج نظام باك-إند معقد أو بوابة دفع بهالمرحلة |

## هيكلة الملفات وشرح كل جزء

```
mazaaj/
├── middleware.ts              # يتعرف على لغة الزائر ويوجهه لـ /ar أو /en
├── i18n/
│   ├── routing.ts             # تعريف اللغتين المدعومتين والافتراضية (ar)
│   ├── navigation.ts          # Link/useRouter/usePathname بنسخة "واعية باللغة"
│   └── request.ts             # يحمّل ملف الترجمة المناسب حسب اللغة الحالية
├── messages/
│   ├── ar.json                # كل نصوص الموقع بالعربي
│   └── en.json                # نفس النصوص بالإنجليزي (لازم يبقوا متطابقين بالمفاتيح)
├── app/
│   ├── globals.css            # إعدادات آيفون (منع الزوم، إخفاء scrollbar)، الخطوط، safe-area
│   └── [locale]/
│       ├── layout.tsx         # الـ layout الجذري: يحمّل الخطوط، يغلف الموقع بـ CartProvider
│       │                        و NextIntlClientProvider، ويعرض Navbar + Footer + CartDrawer
│       ├── page.tsx            # الصفحة الرئيسية: تجمع كل الأقسام بالترتيب
│       ├── menu/page.tsx       # صفحة المنيو التفاعلية (وجهة رابط QR)
│       └── order/page.tsx      # صفحة إتمام الطلب (نموذج البيانات + إرسال واتساب)
├── components/
│   ├── Navbar.tsx              # الشريط العلوي الزجاجي: لوجو، روابط تنقل سلس، مبدّل اللغة، أيقونة السلة، قائمة الجوال
│   ├── Footer.tsx              # تذييل الموقع: روابط سريعة، تواصل، سوشال ميديا
│   ├── LanguageSwitcher.tsx    # زر تبديل AR/EN مع الحفاظ على نفس الصفحة
│   ├── CartDrawer.tsx          # درج جانبي يعرض محتويات السلة ويقود لصفحة /order
│   ├── Hero.tsx                # قسم الافتتاح بالصفحة الرئيسية
│   └── sections/
│       ├── About.tsx           # قسم "من نحن"
│       ├── MenuPreview.tsx     # لمحة عن فئات المنيو + زر يوصل لصفحة /menu
│       ├── GameLounge.tsx      # قسم صالة الألعاب بالطابق الثاني (تصميم داكن مميز)
│       ├── DeliveryCTA.tsx     # قسم يشجع على طلب التوصيل
│       └── ContactSocial.tsx   # العنوان، أوقات الدوام، روابط التواصل الاجتماعي
├── lib/
│   ├── cart-context.tsx        # إدارة السلة بالكامل (إضافة/حذف/تعديل كمية) + حفظها بالمتصفح
│   ├── config.ts                # ⚠️ رقم واتساب الكافيه وروابط السوشال ميديا (حالياً placeholder)
│   ├── motion.ts                # حركة ظهور موحدة (fade-up) تُستخدم بكل الأقسام
│   └── placeholder-menu.ts      # ⚠️ منتجات تجريبية مؤقتة لحد ما توصلنا قائمة المنيو الفعلية
└── tailwind.config.ts           # نظام الألوان (خلفية/بني/ذهبي) والخطوط
```

## أشياء لسه ناقصة (Placeholders) — أحتاجها منك

- ⚠️ **رقم واتساب الكافيه** الحقيقي (بصيغة دولية) — موجود حالياً كرقم وهمي بملف [lib/config.ts](lib/config.ts).
- ⚠️ **روابط حسابات التواصل الاجتماعي** (انستغرام، سناب شات، تيك توك...) — نفس الملف.
- ⚠️ **اللوجو الفعلي** — حالياً مستخدم شعار نصي بسيط ("م" داخل دائرة).
- ⚠️ **قائمة المنيو الحقيقية** (الفئات، الأصناف، الأسعار، وصور إذا متوفرة) — حالياً فيه بيانات تجريبية بملف [lib/placeholder-menu.ts](lib/placeholder-menu.ts).
- **العنوان الفعلي وأوقات الدوام** — موجودة كنص مؤقت بملفات الترجمة (`messages/ar.json` و `messages/en.json`، مفتاح `contact`).

## ملاحظة تقنية

اللغة تُكتشف تلقائياً حسب إعداد المتصفح (Accept-Language) أول ما يفتح الزائر الموقع، وإذا ما قدر يحدد يرجع افتراضياً للعربي. إذا تفضل إن العربي يكون هو الافتراضي دائماً بغض النظر عن إعداد جهاز الزائر، خبرني نعطلها بسطر وحدة بملف `i18n/routing.ts`.
